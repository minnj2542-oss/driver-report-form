# 설정 가이드 (1회성)

카카오 연동 없이, 기사님 제출 → 구글 시트 저장 → 대시보드에서 실시간 확인하는 구조입니다.
본인 계정 로그인이 필요한 단계라 직접 진행해주셔야 합니다. 막히는 부분 있으면 화면 캡처해서 알려주세요.

## 1. Google Apps Script 프로젝트 만들기
1. https://script.google.com 접속 → "새 프로젝트"
2. 기본 `Code.gs` 내용을 전부 지우고, 이 폴더의 `apps-script/Code.gs` 내용을 그대로 붙여넣기
3. 우측 상단 "배포" → "새 배포" → 유형: "웹 앱"
   - 실행 대상: **나**
   - 액세스 권한: **모든 사용자**
4. 배포 후 나오는 웹 앱 URL(`https://script.google.com/macros/s/.../exec`)을 복사해두기 → 3단계에서 씀
   - 처음 요청이 들어오면 스크립트가 자동으로 "기사님 신고 데이터"라는 구글 시트를 만들어줍니다. 미리 시트를 만들어둘 필요 없음.

## 2. 상품 마스터 엑셀 등록 (상품코드 → 상품명/존 자동매칭용)
1. 위 1단계를 한 번 실행하고 나면(웹 앱 URL을 브라우저로 한 번 열어보면) 구글 드라이브에 "기사님 신고 데이터" 시트가 생기고, 그 안에 탭이 2개 있습니다: "신고"(자동 기록, 건드릴 필요 없음) / "상품마스터"
2. "상품마스터" 탭을 열어보면 헤더가 이미 이렇게 되어 있습니다: **상품코드 / 상품명 / 존** (정확히 이 순서, 3열)
3. 준비하실 엑셀도 이 3열(상품코드, 상품명, 존) 순서로 맞춰서, 헤더 없이(또는 헤더 포함해도 상관없이) A2셀부터 붙여넣기
   - "존"은 그 상품이 있는 창고 구역 정보예요. 없는 상품은 빈칸으로 둬도 됨(그냥 매칭 시 존 표시만 빠짐)
4. 기사님이 폼에서 상품코드를 입력하면 이 표를 기준으로 상품명/존이 자동으로 채워집니다. 나중에 상품이 추가되면 이 탭에 행만 추가하면 바로 반영돼요 (재배포 필요 없음).

## 3. 프론트엔드에 웹 앱 URL 연결
1. `index.html` 파일 열어서 `APPS_SCRIPT_URL` 상수를 1-4단계에서 복사한 웹 앱 URL로 교체
2. `dashboard.html` 파일에서도 동일하게 `APPS_SCRIPT_URL`을 같은 주소로 교체
3. GitHub 저장소 생성 후 push → GitHub Pages로 배포 (기존 `coupang-pallet-project`와 동일한 방식)
   - 예: `https://<계정>.github.io/driver-report-form/index.html` (기사님용)
   - 예: `https://<계정>.github.io/driver-report-form/dashboard.html` (우리가 보는 대시보드)
4. 기사님용 `index.html` 주소로 QR코드 생성해서 안내

## 4. 최종 테스트
1. `index.html`에서 실제로 입력 → "전송하기"
2. `dashboard.html`을 열어서 "대기중" 목록에 방금 제출한 신고가 아래 형식으로 뜨는지 확인:
   ```
   12/B방

   220458/햇반/A/2개

   물건 나오지 않아 이동부탁드립니다.
   ```
3. "복사" 버튼으로 클립보드 복사, "처리완료" 버튼으로 완료 처리(회색으로 아래 섹션 이동) 확인
4. 안 되면 Apps Script 에디터의 "실행" 탭(실행 기록)에서 에러 로그 같이 확인해요.

## 참고
- 데이터는 Apps Script가 자동 생성한 구글 시트("기사님 신고 데이터")에 쌓입니다. 필요하면 시트를 직접 열어서 원본 데이터도 확인/백업 가능합니다 (Apps Script 편집기 좌측 "SHEET_ID" 스크립트 속성 값으로 시트를 찾을 수 있음).
- 대시보드는 15초마다 자동 새로고침되고, 탭으로 다시 돌아오면 즉시 새로고침됩니다.
- 현재 대시보드는 URL만 알면 누구나 볼 수 있는 상태입니다(암호 보호 없음, 요청하신 대로). 나중에 필요하면 간단한 암호 보호를 추가할 수 있어요.
